function refreshHeartButton() {
  const heartButton = document.getElementById('bottle-wish-heart-<%= @wish.bottle.id %>');
  const innerHTML = '<%= j render "wishes/heart_button", bottle: @wish.bottle %>';
  heartButton.innerHTML = innerHTML;
  document.querySelector("[data-wish-id='<%= @wish.id %>']").remove();
}

refreshHeartButton();
